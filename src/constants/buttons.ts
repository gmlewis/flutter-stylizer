import * as vscode from 'vscode';

export const BTN_ALIGNMENT: vscode.StatusBarAlignment = vscode.StatusBarAlignment.Left;
export const BTN_PRIORITY: number = 1; // The higher the number the more left the button appears
